[pytest]
# raccogli solo da tests/ — senza, pytest ricorre in venv/ e si pianta
testpaths = tests
markers =
    network: tocca Booking dal vivo — ESCLUSO di default; lancia con: pytest -m network
# di default i test di rete NON girano (sono lenti e dipendono da Booking)
addopts = -m "not network"
